#include "1.golf.h"

int main(int argc, char const *argv[]) {
    golf ann;
    setgolf(ann, "Ann", 24);
    showgolf(ann);

    golf andy;
    setgolf(andy);
    handicap(andy, 20);
    showgolf(andy);
    return 0;
}
